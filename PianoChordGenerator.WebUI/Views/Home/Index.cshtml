@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html>
<!--
    Created by EncouragingApps.com
-->
<head>
    <style>
        h1 {
            color: black;
            font-family: Arial, Helvetica, sans-serif;
            margin-left: 25px;
            line-height: 0px;
        }

        h3 {
            color: black;
            font-family: Arial, Helvetica, sans-serif;
            line-height: 0px;
            margin-left: 25px;
        }

        .canvas {
            border: 1px solid #d3d3d3;
            vertical-align: middle;
        }

        .radio {
            vertical-align: middle;
        }

        .dropdown {
            color: black;
            font-family: Arial, Helvetica, sans-serif;
            margin-left: 25px;
        }
    </style>
</head>
<body>
    <hr />
    <h1>Piano Chord Generator</h1>
    <hr />
    <div id="divChordBuilderWorkspace">

        <h3>Select a Chord:</h3>

        <select name="chords" id="dropdownChords" class="dropdown">
            <option value="cmaj">C Major</option>
        </select>
        <br />
        <br />
        <h3>Root Position</h3>
        <input type="radio" id="radioRoot" name="radioInversion" value="root" class="radio" />
        <canvas id="pianoChart1Canvas" width="281" height="78" class="canvas">
            Your browser does not support the HTML5 canvas tag.
        </canvas>

        <br />

        <h3>1st Inversion</h3>
        <input type="radio" id="radioFirst" name="radioInversion" value="first" />
        <canvas id="pianoChart2Canvas" width="281" height="78" class="canvas">
            Your browser does not support the HTML5 canvas tag.
        </canvas>

        <br />

        <h3>2nd Inversion</h3>
        <input type="radio" id="radioFirst" name="radioInversion" value="second" />
        <canvas id="pianoChart3Canvas" width="281" height="78" class="canvas">
            Your browser does not support the HTML5 canvas tag.
        </canvas>
        <br />

    </div>


    <script>
        var whitekeycolor = "#FF0000";
        var blackkeycolor = "#000000";

        var pianoChart1 = GetElementById("pianoChart1Canvas");
        var pianoChart2 = GetElementById("pianoChart2Canvas");
        var pianoChart3 = GetElementById("pianoChart3Canvas");

        RenderPianoKeyboard(pianoChart1);
        RenderPianoKeyboard(pianoChart2);
        RenderPianoKeyboard(pianoChart3);

        function RenderPianoKeyboard(canvasToRender) {
            var ctx = canvasToRender.getContext("2d");
            ctx.beginPath();
            ctx.fillStyle = whitekeycolor;
            var counter = 0;

            //Render Whitekeys
            for (i = 0; i < 14; i++) {
                counter += 20;
                ctx.rect(0, 0, counter, 75);
            }

            //Render Blackkeys
            ctx.fillStyle = blackkeycolor;
            ctx.fillRect(12, 0, 15, 43);
            ctx.fillRect(34, 0, 15, 43);
            ctx.fillRect(72, 0, 15, 43);
            ctx.fillRect(92, 0, 15, 43);
            ctx.fillRect(112, 0, 15, 43);
            ctx.fillRect(154, 0, 15, 43);
            ctx.fillRect(174, 0, 15, 43);
            ctx.fillRect(214, 0, 15, 43);
            ctx.fillRect(234, 0, 15, 43);
            ctx.fillRect(254, 0, 15, 43);
            ctx.stroke();

            //Render Chord Indicators
        }

        function GetElementById(thiselement) {
            return document.getElementById(thiselement)
        }

    </script>

</body>
</html>
